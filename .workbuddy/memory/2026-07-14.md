# 2026-07-14

## 项目结构梳理（D:\Documents\Notes）
- 这是一个 **HonKit 6.0.3**（GitBook 的维护分支）个人知识库站点，作者 Lawwaikit，标题「我的笔记」，源语言 zh-hans。
- 双写作流：Obsidian（folder-notes + file-explorer-plus 插件，用 `00-README.md` 作文件夹索引）+ HonKit 构建（`_book/` 为编译产物）。
- 关键问题：**SUMMARY.md 已严重过时**——
  - 死链：指向不存在的文件（学习路线图、7天Docker打卡计划、02-日志&监控系统、03-CICD/GitHub Actions）。
  - 缺失真实目录：Jenkins、K8S、Gitlab CI、自动化/Pytest、AI人工智能/MCP、计算机基础子目录均未收录。
  - 章节显示名与真实文件夹不一致（如 "02-测试开发技能" vs 实际 "02-测试工程"；"02-CICD+DevOps" vs 实际 "02-CICD"）。
- 注意 `.gitignore` 忽略 `_book/`、`.obsidian/`、`.idea/`，但 `_book/` 内部另有一个独立的 `.git`（嵌套仓库），属历史遗留。
- `zimages/` 存剪贴板粘贴图片（按时间戳命名）；`styles/website.css` 仅 185B 自定义样式。

## 文档生成器选型调研（用户原需求：gitbook 文档站）
- 原始 `gitbook-cli` / `gitbook` 已于 2021 年底废弃（EOL），Node≥16 跑不动。HonKit 是其活跃维护 fork，2025–2026 持续发版（最新 ~6.2.x），项目仍活着；用户本地 6.0.3 只是偏旧，升级是 drop-in。
- HonKit 短板：维护型 fork、引擎不进化、默认主题老、插件生态弱。
- 替代方案对比：原地升 HonKit 6.2.x（零改动）/ Docsify（_sidebar.md，免构建，最接近 GitBook 手感）/ mdBook（原生用 SUMMARY.md，近零迁移但颜值一般）/ **MkDocs Material（现代标杆，搜索&主题&插件最佳，需把 SUMMARY.md 转成 mkdocs.yml nav）** / VitePress·Docusaurus（偏重）。
- 已向用户给出 A(升 HonKit+修 SUMMARY) / B(迁 MkDocs Material) / C(Docsify·mdBook) 三选项，待用户定方向。无论选哪个，第一步都是先修出一份准确的 SUMMARY.md。

## 已执行：迁移到 MkDocs Material（用户选 B，做 1–5）
- 环境：用受管 Python 3.13 在项目建 `.venv/`（已加 .gitignore），`pip install --no-cache-dir "mkdocs-material[recommended]"` 成功（沙箱里 pip 默认 safe-delete 会因回收站不可用而失败，`--no-cache-dir` 可绕过）。装出 mkdocs 1.6.1 + mkdocs-material 9.7.6。
- `mkdocs.yml`：docs_dir=Notes，theme.material（中文明暗切换/导航特性），markdown_extensions（admonition/pymdownx/toc），plugins: search；**省略 nav** 由 MkDocs 按 index.md 自动生成目录。
- 文件改造（脚本 `migrate.py` 一次性完成，已加 .gitignore）：
  - `zimages/` → `Notes/zimages/`（进入 docs_dir 才能随站复制图片）。
  - 递归重命名 23 个 `00-README.md` → `index.md`（MkDocs 目录首页约定）。
  - 把 3 个 Obsidian 专属 `![[Pasted image…png]]` 嵌入改写成标准 `![](../../zimages/…)`（Obsidian 同样支持，无副作用）。
  - 备份 `.obsidian/plugins/folder-notes/data.json` → `.bak`，并把 `folderNoteName`/`newFolderNoteName` 改为 `index`，使 Obsidian 重命名后仍认 `index.md` 为文件夹首页。
- 构建：`mkdocs build` 成功（51 个 HTML 页，3.55s，无错，仅有 MkDocs 2.0 前瞻性警告）。
- 预览：`mkdocs serve`（端口 8000）后台运行中，已由 present_files 打开 http://127.0.0.1:8000 。
- 注意点：因 docs_dir=Notes，URL 带 `/Notes/` 前缀（功能正常，仅路径略长）；如需更干净根路径，可改结构或将 Notes 内容上移一层。

## 用户反馈：MkDocs Material 布局不喜欢 → 切回 GitBook
- 用户看了 5 套 MkDocs 主题（indigo/emerald/amber/midnight/minimal）后仍嫌「不只是配色，布局也不喜欢」，明确要**切回 GitBook 方案**：更新笔记到 GitHub，gitbook.io 自动同步。
- 线上站：https://lawwaikit.gitbook.io/notes ；GitBook 从 `git@github.com:Lawwaikit/Notes.git` (main) 同步。
- 关键约定（与线上一致，已还原）：**文件夹首页用 `00-README.md`**（非 index.md）；首屏 `introduction.md`；根 `README.md` 为 GitBook 默认 readme；**无 `.gitbook.yaml`**（GitBook 按 SUMMARY.md 渲染）；图片在 `Notes/zimages/`。
- 还原操作（`fix_gitbook.py`，已 gitignore）：23 个 `index.md`→`00-README.md`；恢复 `.obsidian/plugins/folder-notes/data.json.bak`→`00-README`；按真实目录树重写 SUMMARY.md（补齐 Jenkins/K8S/Gitlab CI/Pytest/AI·MCP/计算机基础，移除失效死链）；删 MkDocs 专用 `cozy.css`。
- **Windows 大小写坑（重要）**：用户把 `notes/`（小写）重组/重命名为 `Notes/`（大写）等，git-on-Windows 默认 `core.ignorecase=true` 会把 `Notes/` 折叠成 `notes/`，导致 SUMMARY.md（写大写 `Notes/`）与 GitHub 实际路径（小写）不符、GitBook 找不到页面。→ 解法：暂存/提交时加 `git -c core.ignorecase=false add/rm`，强制保留磁盘大写；旧 `notes/`、`zimages/` 用 `git -c core.ignorecase=false rm --cached -r` 删除。最终提交 `684170c`：48 R + 6 A + 7 D + 2 M，所有重命名/新增目标均为大写 `Notes/`，未误含 `.workbuddy/` 与遗留 `gitbook-plugin-summary.py`。
- **推送被沙箱挡住**：`git push origin main` 在 SSH 22 端口超时；HTTPS 443 能连到 github 但无凭据/无 TTY（`/dev/tty: No such device`）无法鉴权。→ 结论：本地提交已完成，但**必须由用户在本机执行 `git push origin main`**（本机有 SSH key）。GitBook 会在 push 后自动同步。
- 提醒：若 GitBook 后台把内容根目录显式设成了 `notes/`，需改成 `Notes/`（默认仓库根则无需改）。
- 后台仍运行 `mkdocs serve`(8000) 与 `style_preview` 静态服务(8001)，已无用了，可随时停。

## 收尾：停服务 + 清残留 + 改 https + 推送成功
- 已停两个预览进程：`mkdocs serve`(8000，之前已退出) 与 `style_preview` 静态服务(8001，pid 1192，kill 释放端口)。
- 已删除 MkDocs 实验残留（均在 .gitignore 内、未被跟踪、可重现）：`.venv/`、`site/`、`style_preview/`、`fix_gitbook.py`、`gen_styles.py`、`migrate.py`、`mkdocs.yml`，及 5 个风格配置 `mkdocs_{amber-cozy,emerald-geek,indigo-classic,midnight-neon,minimal-clean}.yml`。根目录现仅留项目文件 + 遗留 `gitbook-plugin-summary.py`（非本次产物，未动）。
- **origin 已改为 https**：`git remote set-url origin https://github.com/Lawwaikit/Notes.git`。
- **沙箱内推送成功**（推翻上一轮「必须本机 push」结论）：本环境 `gh` CLI 已登录 github.com（账号 Lawwaikit，协议 https，带 token）。用 `GIT_TERMINAL_PROMPT=0 git -c credential.helper='!gh auth git-credential' push origin main` 即可，token 经 gh 凭据助手注入、**不进 URL/日志**（日志 `To` 行为纯净 https URL，无泄露）。结果 `ba1e4ae..684170c  main -> main`，PUSH_EXIT=0。→ GitBook 已自动同步。
- 可复用经验：本沙箱能连 GitHub 443、且 gh 已认证，故 https push 可行；SSH 22 仍超时。需 push 时优先走 gh credential helper，不要把 token 写进 remote URL 或命令行。

## 清理 HonKit 遗留：删除 book.json + styles/website.css
- 用户要求删本地 HonKit 相关配置/产物，**删除前先列清单确认**。搜索结果：`_book/`（HonKit 编译产物）**已不在本地**（之前某次已清掉，无需处理）；唯一剩余 HonKit 相关项是被 git 跟踪的 `book.json`（GitBook SaaS 不同步它，属本地构建遗留）。`styles/website.css`(185B) 只被 book.json 引用，SaaS 也不同步。
- 经 AskUserQuestion 确认：选项=「从仓库彻底删除 book.json（含 GitHub）」+「一起删 styles/」。
- 执行：`git rm book.json styles/` → 提交 `7deabe8`（D book.json, D styles/website.css）→ `git -c credential.helper='!gh auth git-credential' push origin main` 成功（`684170c..7deabe8 main -> main`，PUSH_EXIT=0）。

## introduction.md 的用途 + 重写 README + 删遗留脚本
- **introduction.md 用途澄清**：根 `README.md` 才是 GitBook SaaS 的站点首页；`introduction.md`（仅 `# Lawwaikit的个人笔记` 一行）被 `SUMMARY.md` 第 3 行 `[介绍](introduction.md)` 引用，承担「站点目录第一项/介绍页」角色。历史上也曾被 HonKit `book.json` 的 `structure.readme` 设为本地构建落地页，HonKit 去掉后仅剩 SUMMARY 第一项这一定位。
- **重写根 README.md**：原内容仅 `# Notes` 一行。已重写为仓库说明 + GitBook 站点首页（含线上链接、目录结构、写作约定 00-README.md / 图片放 Notes/zimages/ 用标准 `![](...)`、发布方式、已清理构建遗留的说明）。提交 `ca7d0a0` 并推送成功（`7deabe8..ca7d0a0 main -> main`）。
- **删除 gitbook-plugin-summary.py**：确认其为未跟踪本地文件（`??`，不在仓库），直接 `rm` 即可，GitHub 无需操作。
- **删除 introduction.md 及其在 SUMMARY.md 的引用**：用户要求。SUMMARY.md 第 3 行 `[介绍](introduction.md)` 移除；`git rm introduction.md`（跟踪文件）。提交 `9f9188e` → push 成功（`ca7d0a0..9f9188e main -> main`，PUSH_EXIT=0）。现仓库根：`Notes/ SUMMARY.md README.md .gitignore .obsidian/ .workbuddy/`（README.md 为 GitBook 站点首页）。已无任何 HonKit / 本地构建痕迹。
- **坑：commit 9f9188e 遗漏 `git add SUMMARY.md`**（只 `git rm introduction.md`），导致远端 SUMMARY.md 仍残留 `[介绍](introduction.md)` 引用，GitBook 目录仍显示「介绍」项（指向已删的 404 文件）。用户反馈"介绍页面还在"。→ 教训：删文件+改引用应一起 `git add` 后再 commit，或提交后用 `git show --stat` 核对提交内容。补交 `f157ebc`（M SUMMARY.md）→ push（`9f9188e..f157ebc`）→ gh api 复核远端 SUMMARY 首项已是「01-编程语言」，介绍引用消失。

## 核对 SUMMARY 与目录一致性 + 跟踪 .memory/.workbuddy
- 用户手动改了文件路径：把 `Notes/上下文快照.md`、`Notes/计划.md` **移到 `.memory/`**（现 `.memory/SDET 学习计划 上下文快照.md`、`.memory/计划.md`）。
- 用只读脚本核对 SUMMARY↔目录：死链仅 2 条（`Notes/上下文快照.md`、`Notes/计划.md`，已不在 Notes/）；`Notes/` 下 48 个 .md 中除 `Notes/00-README.md`（根文件夹 landing 页，历来不在 TOC，与之前一致）外全部被引用；其余 47 条链接全部命中真实文件 ✔。
- 修正：Edit 删除 SUMMARY 第 50–51 行那 2 条死链。
- 跟踪跨设备记忆：`git add .memory/ .workbuddy/` + `git add -u Notes/`。**git 自动识别为 R100 重命名**（Notes/两文件 → .memory/ 两文件，内容 100% 相同，保留历史）；`.workbuddy/` 仅含 `memory/`（16K，无运行时大文件，安全可跟踪）。
- 提交 `ff0f5a6` → push 成功（`f157ebc..ff0f5a6 main -> main`，PUSH_EXIT=0）。SUMMARY 死链已清、`.memory/`/`.workbuddy/` 纳入跟踪，GitBook 会自动同步。
- 注意：`.memory/` 与 `.workbuddy/` 现已被 git 跟踪；`.obsidian/` 仍在 .gitignore 中不被跟踪（符合预期）。

## 清理 GitHub Pages 残留（gh-pages 分支 + pages workflow）
- 用户要求删除本地+远端 `gh-pages` 分支与 GitHub 上对应 workflow（当前发布已改走 GitBook SaaS，这些是无用残留）。
- 远端确实有 `gh-pages` 分支（commit `23cad05…`）；本地无检出的 `gh-pages`，仅有陈旧远程跟踪引用 `origin/gh-pages`。
- GitHub 上的 workflow 是 `pages-build-deployment`（path `dynamic/pages/pages-build-deployment`，id `187175893`），属 **GitHub 托管的内置动态 Pages 部署 workflow**，仓库内无 `.github/workflows/*.yml` 文件。
- 执行结果：
  - `git push origin --delete gh-pages` 成功 ✅（BRANCH_DEL_EXIT=0），远端分支已删。
  - `git branch -dr origin/gh-pages` + `git fetch --prune origin` 清理本地陈旧跟踪引用 ✅（`branch -dr` 报 not found 是因为 prune 已先行移除）。
  - `gh api -X DELETE /repos/.../pages`（禁用 Pages）首次返回 422 "not allowed"，但删除 gh-pages 分支后 `GET /pages` 变 **404=已禁用** ✅（大概率分支删除触发了 Pages 关停）。
  - 尝试用 API `PUT/DELETE /actions/workflows/187175893` 禁用/删除该 workflow → 均返回 **404 Not Found**，证实其为 GitHub 托管动态 workflow，**用户无法通过 API 改动或删除**。现 `state=active` 仅为 UI 残留，因 Pages 已关、`gh-pages` 源分支已删，它**不会再被触发**。
- 结论：gh-pages 分支（本地+远端）已彻底删除，Pages 站点已禁用；`pages-build-deployment` 条目残留但已失效，无法通过 API 移除，唯一彻底消除路径是 GitHub 网页端 Settings（其会自动随 Pages 禁用而清理，或等 GitHub 最终回收）。无需用户再操作，除非想手动去 UI 里确认。
